(bool yes) in_array(array stuff, string key):
    for element in stuff:
        if element == key:
            yes = true
            return

(object b) copy_object(object a):
    text = to_json(a)
    b = from_json(text)
